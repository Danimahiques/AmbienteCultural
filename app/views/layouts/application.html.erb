<!DOCTYPE html>
<html>
  <head>
    <title>Userapp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  
  <style>
ul {
  list-style-type: none;
padding-top: 20px;
overflow: hidden;
background-color: #333;
height: 70px;
padding-left: 180px;
}

li {
  float: left;
}

li a {
  display: block;
  color: white;
  padding: 14px 60px;
  text-decoration: none;
}

li a:hover:not(.active) {
  background-color: red;
}

.active {
  background-color: white;
}
li.dropdown {
  display: inline-block;
}

.dropdown-content {
  display: none;
  position: absolute;
  background-color: white;
  min-width: 160px;
  box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
  z-index: 1;
}

.dropdown-content a {
  color: black;
  padding: 12px 16px;
  text-decoration: none;
  display: block;
  text-align: left;
}

.dropdown-content a:hover {background-color: white;}

.dropdown:hover .dropdown-content {
  display: block;
}
img{
    width:190px;
    height:90px;
    position: absolute;
    left: 8px;
    top:16px;
}
#user_nav{
    position: relative;
    left: 33vw;
}
</style>
  </head>

  <body>
  <ul>
  <img src="/images/logo2.PNG">
  <li><a href="/">Home</a></li>
  <% if user_signed_in? %>
  <li><a href="/users">Usuarios</a></li>
  <% else %>
    <li><a href="/users/sign_up">Registro</a></li>
  <li><a href="/users/sign_in">Login</a></li>
  <% end %>
  
 
  
  <% if user_signed_in? %>
  <li class="dropdown">
    <a href="/peliculas" class="dropbtn">Peliculas</a>
    <div class="dropdown-content">
      <a href="/peliculas/new">Añadir Peliculas</a>
    </div>
  </li>
   <li class="dropdown">
    <a href="/libros" class="dropbtn">Libros</a>
    <div class="dropdown-content">
      <a href="/libros/new">Añadir Libros</a>
    </div>
  </li>
  <% end %>
   <% if user_signed_in? %>
    <li><%= link_to "Cerrar Sesión", destroy_user_session_path, :method => :delete%></li>
  <% else %>

  <% end %>
</ul>
<div id="user_nav" style="">
  <% if user_signed_in? %>
    Conectado como <%= current_user.email %>. No eres t&uacute;?
    <%= link_to "Log off", destroy_user_session_path, :method => :delete%>
  <% else %>
   
  <% end %>
</div> 
    <p style="position: relative;
              left: 36vw;" class="notice"><%= notice %></p>
 <p style="position: relative;
            left: 36vw;" class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
